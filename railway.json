{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "gunicorn CalorieApp:app --bind 0.0.0.0:$PORT",
    "releaseCommand": "python init_db_migration.py && flask db upgrade"
  }
}